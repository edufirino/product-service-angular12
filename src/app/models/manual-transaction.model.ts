export interface ManualTransaction {
  month: number;
  year: number;
  entryNumber: number;
  productCode: string;
  cosifCode: string;
  description: string;
  transactionDate: string;
  userCode: string;
  amount: number;
}
